package Graphs;

import Binary_Practice.BinaryTree;
import Binary_Practice.TreeNode;

public class Depth_first_traversal_for_graph {
    vector<int> ans;
    public void solve(TreeNode node, int start){
        vector<int> 
    }

    public static void main(String[] args) {
        int[] a = {1, 2, 3, 4, 5, 6, 7};
        int n = 
        TreeNode root = BinaryTree.buildTree(a);

        Depth_first_traversal_for_graph Bi = new Depth_first_traversal_for_graph();
        Bi.solve(root, 0);
    }
}
